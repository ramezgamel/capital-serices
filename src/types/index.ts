export type Teacher = {
  name: string;
  phoneNumber: string;
  location: string;
  rate?: number;
  image?: string;
  subject: string;
};

export type Shop = {
  name: string;
  open: string;
  close: string;
  phoneNumber: string;
  location: string;
  image: string;
};
